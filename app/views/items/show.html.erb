<p id="notice"><%= notice %></p>
<div class="container hr">
  <%= link_to t('global.edit'), edit_item_path(@item), class: "btn btn-primary" %> |
  <%= link_to t('global.back'), items_path, class: "btn btn-primary" %>
  <p>
    <strong><%= t('items.name') %>:</strong>
    <%= @item.name %>
  </p>

  <p>
    <strong><%= t('items.quantity') %>:</strong>
    <%= @item.quantity %>
  </p>
</div>